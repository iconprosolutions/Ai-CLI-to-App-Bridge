{
  "name": "provider-bridge",
  "version": "1.0.0",
  "description": "OpenAI-style HTTP provider facade that routes to the Claude and Gemini HTTP bridges.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
